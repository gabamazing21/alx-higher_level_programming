#include "list.h"
int is_palindrome(listint_t **head)
{
	listint_t *h;
	h = *head;
	/* getting the length of the node */
	int i = 0, j = 0, half = 0;
	while (h->next != NULL)
	{
		h = h->next;
		i++
	};

	for (j = 0; head->next ! NULL: j++)
	{
		half = i/2;
		if (
	};
}
